<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.cmc.android.utils.ApplicationClass"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CMC_Android"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".LoginDetailActivity" />
        <activity android:name=".FindPwdActivity" />
        <activity android:name=".FindPwdNumberActivity" />
        <activity android:name=".ChangePwdActivity" />
        <activity android:name=".SignupFirstActivity" />
        <activity android:name=".SignupSecondActivity" />
        <activity android:name=".SignupThirdActivity" />
        <activity android:name=".SignupCompleteActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>