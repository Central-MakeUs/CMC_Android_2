<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CMC_Android" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Titlebar 제거 부분 -->
        <item name="windowNoTitle">true</item>
        <!-- 상태바 투명 설정 부분 -->
        <item name="android:windowTranslucentStatus">true</item>
        <!-- 기본 Splash 제거 -->
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="S">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- BottomSheet 키보드 위로 올리기 -->
    <style name="DialogStyle" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="android:windowIsFloating">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
    </style>

    <style name="Theme.CMC_Android" parent="Base.Theme.CMC_Android" />
</resources>